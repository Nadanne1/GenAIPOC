# MCP Integration Requirements - Verified Working Versions
# Core MCP package (REQUIRED for FastMCP)
mcp>=1.15.0

# AgentCore CLI (REQUIRED for deployment)
bedrock-agentcore-starter-toolkit>=0.1.13

# AWS SDK (REQUIRED for Cognito authentication)
boto3>=1.34.0
botocore>=1.34.0

# HTTP client (used by MCP client)
requests>=2.31.0

# Development and testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0